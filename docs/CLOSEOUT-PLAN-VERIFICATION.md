# 收尾规划页面检查 · 2026-09-25

本批只统一规划、范围和验收记录，没有新增天体或重算物理验证。

## 已检查

- 328 项自动检查、79 个测试文件通过；生产构建通过。
- 四份规划文档与网页共用内容台账，构建时检查同步，兼容 Windows 换行。
- 浏览器实测默认收尾页包含 5 步、4 项未关闭事项、25 条核对记录。
- E18 核对记录能定位内容卡、进入现有恒星视图并返回规划。
- 六个规划页签可用；下载文件包含全部核对项和已知差距。
- 打开当前验收记录不覆盖旧版浏览器记录，不自动代替用户验收。
- 1600×1000 和 390×844 检查：步骤卡片可读，窄屏无页面横向溢出；正常路径未捕获未处理异常。

复现：先构建并在 4180 启动预览，使用 9223 调试浏览器打开预览，再运行 `node scripts/qa/closeout-plan.mjs`。测试截图写入本地 `.cache/closeout-desktop.png`、`.cache/closeout-narrow.png`。

## 未完成与限制

这些是规划页面和入口检查，不是重新完成 25 类内容的视觉复验。后续仍需按 C02 逐类检查主全景、近景、说明和返回是否一致。

主全景星表背景差距 F01、资源预算 F02、实际设备性能 F03、用户总验收 F04 均保持未关闭。生产构建仍提示主包偏大，本批不宣称已解决资源下载或帧率问题。

390px 下规划内容可读，但站点全局顶部导航仍拥挤、个别入口被裁切；本次窄屏检查不等于移动端整站验收。首版目标仍为电脑浏览器，该现象保留在体验限制中。
